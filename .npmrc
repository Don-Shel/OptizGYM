# Neon Auth UI currently publishes peer ranges that npm 11 resolves incompatibly
# with the beta Neon Auth client. The tested dependency graph is valid; use the
# same compatibility mode in CI/Vercel as local verification.
legacy-peer-deps=true
